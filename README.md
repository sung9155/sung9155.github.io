# 시뮬레이터 모음

브라우저에서 바로 돌아가는 정적 시뮬레이터 모음 · https://sung9155.github.io/

빌드 도구·외부 CDN 없음. HTML/CSS/바닐라 JS만 사용하며 모든 계산은 클라이언트에서 끝난다.

## 페이지

| 페이지 | 내용 |
|---|---|
| `index.html` | 랜딩 (시뮬레이터 목록) |
| `lotto.html` | 로또 6/45 구매 시뮬레이터 |
| `loan.html` | 대출 상환 계산기 |

### 로또 구매 시뮬레이터
- 구입 매수(최대 500만 장), 번호 선택 방식: 자동(랜덤) / 수동(고정 6개) / 반자동(고정 1~5개 + 나머지 자동)
- 추첨 방식: 장마다 새 회차 / 한 회차 고정
- 구매 간격: 0.1초 · 0.05초 · 0.01초 · 즉시(일괄 계산)
- 실시간 집계: 등수별 당첨 수, 총 구입액·당첨금·손익·회수율, 최고 등수
- 등수별 당첨금 합계 막대그래프, 누적 구입액 vs 누적 당첨금 곡선
- 이론 확률(1등 1/8,145,060 등)과 실측 확률 비교표, 당첨 기록 200건
- 당첨금은 회차마다 달라지므로 기본값은 평균 추정치이며 직접 수정 가능

### 대출 상환 계산기
- 상환 방식: 원리금균등 · 원금균등 · 만기일시
- 대출금액, 연이자율, 기간(개월), 거치기간(이자만 납부), 매월 추가 원금상환
- 요약: 첫 달 납입금, 총 이자, 총 상환액, 실제 상환 개월수(추가상환 시 단축 표시)
- 월별 원금/이자 스택 막대그래프(180회차 초과 시 연 단위 집계), 잔금 곡선
- 상환 방식 3종 비교표(첫 달·마지막 달 납입금, 총 이자, 이자 비중)
- 월별/연도별 상환표, CSV 내보내기
- 월 복리(연이율 ÷ 12) 기준 단순 계산. 실제 상품의 일수 계산·수수료와 다를 수 있음

## 구조

```
index.html, lotto.html, loan.html
assets/css/base.css     테마 토큰(라이트/다크) + 컴포넌트
assets/js/ui.js         테마 토글, 숫자 포맷, CSV, 세그먼트 컨트롤
assets/js/chart.js      의존성 없는 SVG 차트(라인/막대/스택 막대) + 호버 툴팁
assets/js/lotto.js      로또 시뮬레이션
assets/js/loan.js       대출 상환 스케줄 계산
```

## 로컬 실행

```bash
python -m http.server 8765
# http://localhost:8765
```

## 브랜치

- `main` — 현재 사이트
- `legacy-site` — 이전 "생애 자산 시뮬레이터"(단타/취업/시드 등 탭) 보관
